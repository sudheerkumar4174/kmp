class Solution(object):
    def scoreOfString(self, s):
         sc=0
         for i in range(len(s)-1):
            a=ord(s[i])
            b=ord(s[i+1])
            sc=sc+abs(a-b)
         return sc
