class Solution(object):
    def countNegatives(self, grid):
        m=len(grid)
        n=len(grid[0])
        c=0
        for i in range(m):
            for j in range(n):
                if grid[i][j]<0:
                    c=c+1
        return c


        
